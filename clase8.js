var x = 4, y = '4'
x == y // true == lleva los valores que comparamos a un mismo valor de datos //
x === y // false === X es un num y Y una letra el triple igual compara tipos de datos// 
 
var dami = {
nombre: 'dami'

}

var otraPersona = {
    nombre: 'sacha'
}

// Si comparamos dos objetos//

dami == otraPersona // nos va a decir que es False.//
dami === otraPErsona // ya que Js cuando compara objetos utiliza la referencia de la variable para realizar la comparacion.//

var otraPersona = {
...sacha

}